# Quiet the noisy Kafka client / Testcontainers INFO logging during tests so command output and
# the live demo are readable. Test scope only — the shipped jar is unaffected.
org.slf4j.simpleLogger.defaultLogLevel=warn
org.slf4j.simpleLogger.showThreadName=false
org.slf4j.simpleLogger.showLogName=false
